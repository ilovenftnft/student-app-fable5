# 待确认（家长看这一个文件）

> 开发过程中"不确定就跳过"的事都记在这里，每条写清：是什么、我暂时怎么做的、你要决定什么。确认后我改，改完删条目。

## 2026-09-01

### 2. 引导式回想的耗时基准与"会/不会"四档阈值
- 四档推断：不会 → Again；会且快于基准 0.6 倍 → Easy；≤ 1.5 倍 → Good；更慢 → Hard（`src/server/scheduler/rating.ts`）。基准 = 该条目最近 ≥3 次作答的中位耗时，不足时用种类基准表（`policy.ts`：默写 20s、概念 15s、答题模板 40s、文言注释 12s、单词 8s、听写 10s、错题 90s）。
- 这些数字都是假设，上线两周后用实测替换。**不用现在决定**，只是让你知道。

### 3. 情境卡解锁阈值
- 接句卡 FSRS 间隔 ≥ 30 天视为"成熟"，情境卡才出现。30 天是我定的，没有文献依据（研究 02 只说"接句卡归档后出现"）。可改 `policy.ts` 的 `ARCHIVE_INTERVAL_DAYS`。

### 4. 内容启用日（intro_day 的第 0 天）
- 暂定 **2026-09-07**（开学日），存在 `setting.content_start`。若想开学前一周先跑起来，改成 09-01 即可（首日到期量会因此多一点，模拟里第 1–30 天中位 6.9 分钟）。

### 11. 收件箱 Codex 实跑（2026-09-01，`exams/pages/2026数学-12.png`）
- 新 schema/提示词下 32 秒识出 6 题，科目、带图（2/4/5/7）、知识点都对；`grade_chapter` 有一处标成"七年级下册 相交线与平行线/图形的运动"（对称应在八上/九上），确认时靠你改。
- 手写作答与老师 ✗ 标记还没测过（等开学后的真作业）。

### 14. MVP 六项验收标准仍标"草案"
- `AGENTS.md`「MVP 六项与验收标准」标题仍是"（草案，待家长确认后生效）"。第 1、2 项已按 2026-09-01 晚的决定改写。**你要决定**：整表看一遍没问题就去掉"草案"字样。

## 2026-09-01 晚（家长离开期间做完的，需要你核一眼）

### 15. 道法三句考点：已加 2 条要点，等你翻书后生效
- 第①句"积极的人生态度体现为认真务实"原来就有要点（`文` 是这句，`出处` 用的是紧接着的原句"认真务实的人，往往有着严谨的态度……"），不重复加。
- 第②句加在「在奉献中成就精彩人生」，第③句加在「正确对待顺境和逆境」。两条的出处目前核不上（OCR 错字 / 跨页），放在 `content/chapters/道法-七上.json` 的 **`待核对例外`** 键下，入库时被跳过（`npm run load:chapters` 会报"核不上 2"）。
- **你要做的**：翻七上第 97 页倒数第二段核"融入"、第 93 页末行接第 94 页首行核"在逆境中前行，需要鼓足勇气，迎难而上"三句连续；对的话把 `待核对例外` 改名为 `出处例外`，再跑 `npm run load:chapters`。

### 16. 语文"读读写写"要点（240 条已入库），四处待核
- 第 9 课《从百草园到三味书屋》OCR 只识出 11 个词（教材约 21 个：确凿、轻捷、云霄、和蔼、恭敬、质朴、淋漓、盔甲、绅士等缺），"宛跪拗"的"宛"疑为"觅"的错字已跳过。**请对照原书补齐**（直接在 JSON 该课 `要点` 里加，`文` = `出处` = 词）。
- 第 22 课 OCR"称取"按"称职"收入；第 24 课"虛荣"按"虚荣"收入。请确认。
- 第 24 课《寓言四则》含两篇文言，但"读读写写"栏属整课，加了 7 条；按"文言课不加"应删。**你定**。
- 单字词（嗡、镶、蒂、梗……）核对只是单字匹配，验证力弱，属预期。

### 17. 英语单元字段与要点（已入库）
- 206 条本册新词的 `单元` 已由 `scripts/fill_vocab_units.py` 补齐。脚本用了本机 poppler 的 `pdftohtml`（txt 版词表双栏交错，页码对不上）；这是家长机器上的一次性脚本，孩子电脑不需要 poppler（技术栈约定不变）。
- Unit 3、Unit 6 的 Grammar Focus 是分栏挖空，只取了完整单句而非问答对。Starter Unit 1 选进了 `unit` 这个词，可换。
- `pools.ts` 里 `Word.单元` 类型标 `number|null`，实际是字符串（原有小学段已如此），待顺手改类型。

### 18. 两处按我的理解实现，你看是否对
- **第 5 条"超过 30 分钟休息"**：实现为第 30 分钟时还有事没做完就插 3 分钟休息，30 分钟内做完的不打断；休息后才显示"可以结束了，也可以继续"。硬约束 6 已改写。
- **"再多 5 张"**：10 分钟预算之外再补 5 张（先补顺延的到期卡，再补等待的新卡），仍受 60 分钟硬停约束。固定 5，不给孩子改数。
